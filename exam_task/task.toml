version = "1.0"

[metadata]
author_name = "harbor-exam"
author_email = "harbor-exam@example.com"
author_organization = "harbor-exam"
difficulty = "medium"
difficulty_explanation = "Requires familiarity with Harbor task layout, build context isolation, and the verifier reward protocol."
solution_explanation = "Move solution/ and tests/ out of environment/, fix Dockerfile COPY semantics for both app/ and data/, set executable bits, write reward.txt unconditionally."
verification_explanation = "Verifier checks Harbor framework compliance: build-context cleanliness, image filesystem layout, reward.txt presence and value, absolute paths in instruction.md."
category = "software_engineering"
tags = ["harbor", "framework", "docker", "log-analysis"]
expert_time_estimate_min = 60
junior_time_estimate_min = 240
expert_time_estimate_hours = 1
junior_time_estimate_hours = 4

[verifier]
timeout_sec = 120.0

[agent]
timeout_sec = 120.0

[environment]
build_timeout_sec = 600.0
cpus = 1
memory_mb = 1024
storage_mb = 4096
gpus = 0

[verifier.env]

[solution.env]
